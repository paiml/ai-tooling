[package]
name = "readme2ttl"
version = "0.1.0"
edition = "2021"
description = "Turn a README's frontmatter into Turtle so a SHACL shape can judge it"
publish = false

[[bin]]
name = "readme2ttl"
path = "src/main.rs"

[dependencies]
sha2 = "0.10"

[profile.release]
opt-level = 3
