# Pinned external tool. Verified by `make tools` before anything is unpacked.
#
# Jena 5.6.0 and not 6.x: 6.2.0's classes are Java 21 (class file 65), 5.6.0's
# are Java 17 (class file 61). 17 is what this demo asks for, so 5.6.0 is the
# newest release that runs on the stated floor.
#
# sha512 was checked against apache-jena-5.6.0.tar.gz.sha512 on the Apache
# archive at pin time; the sha256 below is what the Makefile enforces.
jena_version = 5.6.0
jena_sha256  = b39aa6cca06b103ed4d509b42eee512394088859df2b5367a858927bf1fce728
jena_url     = https://archive.apache.org/dist/jena/binaries/apache-jena-5.6.0.tar.gz
